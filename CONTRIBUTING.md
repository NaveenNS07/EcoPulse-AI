# Contributing to EcoPulse (CONTRIBUTING.md)

1. Use Git feature branches.
2. Ensure you adhere to the SOLID Principles.
3. Don't push API keys or sensitive env variables to Source Control.
4. Run `npm run lint` and `npx vitest run` before submitting a PR.
